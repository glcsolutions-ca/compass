name: Release Stage
run-name: Release Stage (${{ inputs.candidate_id }})

on:
  workflow_dispatch:
    inputs:
      candidate_id:
        description: Release candidate id (sha-<40-char-source-sha>)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: production-release
  cancel-in-progress: false

env:
  MANIFESTS_REPO: ghcr.io/glcsolutions-ca/compass-release-manifests
  RELEASE_UNITS_REPO: ghcr.io/glcsolutions-ca/compass-release-units
  ACCEPTANCE_PREDICATE_TYPE: https://compass.glcsolutions.ca/pipeline/attestations/acceptance/v1
  RELEASE_PREDICATE_TYPE: https://compass.glcsolutions.ca/pipeline/attestations/release/v1

jobs:
  resolve_candidate:
    name: Resolve release candidate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    outputs:
      run_release: ${{ steps.resolve.outputs.run_release }}
      skip_reason: ${{ steps.resolve.outputs.skip_reason }}
      candidate_id: ${{ steps.resolve.outputs.candidate_id }}
      source_revision: ${{ steps.resolve.outputs.source_revision }}
      release_unit_digest_hint: ${{ steps.resolve.outputs.release_unit_digest_hint }}
    steps:
      - name: Checkout release resolver scripts
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup ORAS
        uses: oras-project/setup-oras@22ce207df3b08e061f537244349aac6ae1d214f6 # v1

      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve candidate context
        id: resolve
        run: |
          set -euo pipefail

          candidate_id="${{ inputs.candidate_id }}"
          if ! echo "${candidate_id}" | grep -Eq '^sha-[0-9a-f]{40}$'; then
            echo "Invalid candidate_id: ${candidate_id}" >&2
            exit 1
          fi

          pull_dir="${RUNNER_TEMP}/release-candidate-pull"
          rm -rf "${pull_dir}"
          mkdir -p "${pull_dir}"
          oras pull "${MANIFESTS_REPO}:${candidate_id}" -o "${pull_dir}"

          manifest_file="${pull_dir}/manifest.json"
          if [ ! -f "${manifest_file}" ]; then
            manifest_file="$(find "${pull_dir}" -maxdepth 3 -type f -name '*.json' | head -n 1 || true)"
          fi

          if [ -z "${manifest_file}" ] || [ ! -f "${manifest_file}" ]; then
            echo "No manifest JSON found in GHCR candidate package ${MANIFESTS_REPO}:${candidate_id}" >&2
            exit 1
          fi

          cp "${manifest_file}" "${RUNNER_TEMP}/release-candidate.json"
          pnpm --filter @compass/pipeline-tools run manifest:validate:runtime --file "${RUNNER_TEMP}/release-candidate.json"

          source_revision="$(jq -r '.source.revision // empty' "${RUNNER_TEMP}/release-candidate.json")"
          release_unit_digest_hint="$(jq -r '.provenance.releaseUnitDigest // empty' "${RUNNER_TEMP}/release-candidate.json")"

          if ! echo "${source_revision}" | grep -Eq '^[0-9a-f]{40}$'; then
            echo "Invalid source.revision in resolved manifest: ${source_revision}" >&2
            exit 1
          fi

          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "${source_revision}" origin/main >/dev/null 2>&1; then
            echo "run_release=false" >> "$GITHUB_OUTPUT"
            echo "skip_reason=stale candidate source revision not merged to main" >> "$GITHUB_OUTPUT"
            echo "candidate_id=${candidate_id}" >> "$GITHUB_OUTPUT"
            echo "source_revision=${source_revision}" >> "$GITHUB_OUTPUT"
            echo "release_unit_digest_hint=${release_unit_digest_hint}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "run_release=true" >> "$GITHUB_OUTPUT"
          echo "skip_reason=" >> "$GITHUB_OUTPUT"
          echo "candidate_id=${candidate_id}" >> "$GITHUB_OUTPUT"
          echo "source_revision=${source_revision}" >> "$GITHUB_OUTPUT"
          echo "release_unit_digest_hint=${release_unit_digest_hint}" >> "$GITHUB_OUTPUT"

      - name: Write resolver summary
        run: |
          {
            echo "## Release Resolver"
            echo "- Candidate: ${{ steps.resolve.outputs.candidate_id }}"
            echo "- Source revision: ${{ steps.resolve.outputs.source_revision }}"
            echo "- Run release: ${{ steps.resolve.outputs.run_release }}"
            if [ -n "${{ steps.resolve.outputs.skip_reason }}" ]; then
              echo "- Skip reason: ${{ steps.resolve.outputs.skip_reason }}"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  release:
    name: Release Stage
    needs:
      - resolve_candidate
    if: ${{ needs.resolve_candidate.outputs.run_release == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment:
      name: production
      url: ${{ vars.PRODUCTION_WEB_BASE_URL }}
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
      artifact-metadata: write

    steps:
      - name: Capture release start time
        id: release_start
        run: echo "released_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - &checkout_step
        name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ needs.resolve_candidate.outputs.source_revision }}
          fetch-depth: 1
          persist-credentials: false

      - &setup_pnpm_step
        name: Setup pnpm
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4

      - &setup_node_step
        name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22

      - &install_dependencies_step
        name: Install dependencies
        run: pnpm install --frozen-lockfile

      - &setup_oras_step
        name: Setup ORAS
        uses: oras-project/setup-oras@22ce207df3b08e061f537244349aac6ae1d214f6 # v1

      - &login_ghcr_step
        name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch release manifest from GHCR
        run: |
          pnpm --filter @compass/pipeline-tools run manifest:fetch -- \
            --candidate-id "${{ needs.resolve_candidate.outputs.candidate_id }}" \
            --registry-repo "${{ env.MANIFESTS_REPO }}" \
            --out "$RUNNER_TEMP/release-candidate.json"

      - name: Validate release manifest
        run: pnpm --filter @compass/pipeline-tools run manifest:validate --file "$RUNNER_TEMP/release-candidate.json"

      - name: Resolve and verify release-unit digest
        id: release_unit
        run: |
          set -euo pipefail

          resolved_digest="$(oras manifest fetch --descriptor "${RELEASE_UNITS_REPO}:${{ needs.resolve_candidate.outputs.candidate_id }}" | jq -r '.digest')"
          if [ -z "${resolved_digest}" ] || [ "${resolved_digest}" = "null" ]; then
            echo "Unable to resolve release-unit digest for ${{ needs.resolve_candidate.outputs.candidate_id }}" >&2
            exit 1
          fi

          resolved_hint="${{ needs.resolve_candidate.outputs.release_unit_digest_hint }}"
          if [ -n "${resolved_hint}" ] && [ "${resolved_hint}" != "${resolved_digest}" ]; then
            echo "Release-unit digest mismatch between resolved context and registry descriptor." >&2
            echo "resolved=${resolved_hint} descriptor=${resolved_digest}" >&2
            exit 1
          fi

          manifest_hint="$(jq -r '.provenance.releaseUnitDigest // empty' "$RUNNER_TEMP/release-candidate.json")"
          if [ -n "${manifest_hint}" ] && [ "${manifest_hint}" != "${resolved_digest}" ]; then
            echo "Release-unit digest mismatch between GHCR manifest and registry descriptor." >&2
            echo "manifest=${manifest_hint} resolved=${resolved_digest}" >&2
            exit 1
          fi

          echo "release_unit_digest=${resolved_digest}" >> "$GITHUB_OUTPUT"

      - name: Verify acceptance attestation bundle
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh attestation verify \
            "oci://${{ env.RELEASE_UNITS_REPO }}@${{ steps.release_unit.outputs.release_unit_digest }}" \
            --repo "${{ github.repository }}" \
            --predicate-type "${{ env.ACCEPTANCE_PREDICATE_TYPE }}" \
            --bundle-from-oci \
            --signer-workflow "${{ github.repository }}/.github/workflows/02-acceptance-stage.yml" \
            --format json > "$RUNNER_TEMP/acceptance-attestation-verification.json"

      - name: Enforce acceptance attestation business rule
        run: |
          pnpm --filter @compass/pipeline-tools run attest:verify-acceptance -- \
            --verification-json "$RUNNER_TEMP/acceptance-attestation-verification.json" \
            --predicate-type "${{ env.ACCEPTANCE_PREDICATE_TYPE }}" \
            --candidate-id "${{ needs.resolve_candidate.outputs.candidate_id }}" \
            --source-revision "${{ needs.resolve_candidate.outputs.source_revision }}"

      - name: Assert required production environment config
        env:
          API_BASE_URL: ${{ vars.PRODUCTION_API_BASE_URL }}
          WEB_BASE_URL: ${{ vars.PRODUCTION_WEB_BASE_URL }}
          AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
          AZURE_CONTAINERAPP_API_NAME: ${{ vars.ACA_API_APP_NAME }}
          AZURE_CONTAINERAPP_WEB_NAME: ${{ vars.ACA_WEB_APP_NAME }}
          AZURE_CONTAINERAPP_WORKER_NAME: ${{ vars.ACA_WORKER_APP_NAME }}
          AZURE_MIGRATION_JOB_NAME: ${{ vars.ACA_MIGRATE_JOB_NAME }}
          ACR_NAME: ${{ vars.ACR_NAME }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_DEPLOY_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
        run: |
          pnpm --filter @compass/pipeline-tools run env:assert -- \
            --required API_BASE_URL \
            --required WEB_BASE_URL \
            --required AZURE_RESOURCE_GROUP \
            --required AZURE_CONTAINERAPP_API_NAME \
            --required AZURE_CONTAINERAPP_WEB_NAME \
            --required AZURE_CONTAINERAPP_WORKER_NAME \
            --required AZURE_MIGRATION_JOB_NAME \
            --required ACR_NAME \
            --required AZURE_CLIENT_ID \
            --required AZURE_TENANT_ID \
            --required AZURE_SUBSCRIPTION_ID

      - name: Azure login (OIDC)
        uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2
        with:
          client-id: ${{ secrets.AZURE_DEPLOY_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Resolve blue/green slot baseline
        id: slot_context
        env:
          AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
          AZURE_CONTAINERAPP_API_NAME: ${{ vars.ACA_API_APP_NAME }}
          AZURE_CONTAINERAPP_WEB_NAME: ${{ vars.ACA_WEB_APP_NAME }}
        run: |
          node pipeline/stages/05-release/scripts/resolve-blue-green-slot.mjs \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
            --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
            --out "$RUNNER_TEMP/blue-green-slot.json" \
            --github-output "$GITHUB_OUTPUT"

      - name: Assert candidate is currently rehearsed
        env:
          AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
          AZURE_CONTAINERAPP_API_NAME: ${{ vars.ACA_API_APP_NAME }}
          AZURE_CONTAINERAPP_WEB_NAME: ${{ vars.ACA_WEB_APP_NAME }}
          INACTIVE_LABEL: ${{ steps.slot_context.outputs.inactive_label }}
        run: |
          pnpm --filter @compass/pipeline-tools run release:assert-rehearsed -- \
            --manifest "$RUNNER_TEMP/release-candidate.json" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
            --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
            --inactive-label "$INACTIVE_LABEL"

      - name: Promote candidate, smoke prod, and cleanup revisions
        id: release_run
        env:
          AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
          AZURE_CONTAINERAPP_API_NAME: ${{ vars.ACA_API_APP_NAME }}
          AZURE_CONTAINERAPP_WEB_NAME: ${{ vars.ACA_WEB_APP_NAME }}
          AZURE_CONTAINERAPP_WORKER_NAME: ${{ vars.ACA_WORKER_APP_NAME }}
          AZURE_MIGRATION_JOB_NAME: ${{ vars.ACA_MIGRATE_JOB_NAME }}
          ACR_NAME: ${{ vars.ACR_NAME }}
          ACR_LOGIN_SERVER: ${{ format('{0}.azurecr.io', vars.ACR_NAME) }}
          SOURCE_REGISTRY_USERNAME: ${{ github.actor }}
          SOURCE_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          API_BASE_URL: ${{ vars.PRODUCTION_API_BASE_URL }}
          WEB_BASE_URL: ${{ vars.PRODUCTION_WEB_BASE_URL }}
          ACTIVE_LABEL: ${{ steps.slot_context.outputs.active_label }}
          INACTIVE_LABEL: ${{ steps.slot_context.outputs.inactive_label }}
          INACTIVE_API_BASE_URL: ${{ steps.slot_context.outputs.inactive_api_base_url }}
          INACTIVE_WEB_BASE_URL: ${{ steps.slot_context.outputs.inactive_web_base_url }}
        run: |
          set +e

          mkdir -p .artifacts/release

          pnpm --filter @compass/pipeline-tools run release:promote:production -- \
            --manifest "$RUNNER_TEMP/release-candidate.json" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
            --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
            --worker-app-name "$AZURE_CONTAINERAPP_WORKER_NAME" \
            --migrations-job-name "$AZURE_MIGRATION_JOB_NAME" \
            --active-label "$ACTIVE_LABEL" \
            --inactive-label "$INACTIVE_LABEL" \
            --inactive-api-base-url "$INACTIVE_API_BASE_URL" \
            --inactive-web-base-url "$INACTIVE_WEB_BASE_URL" \
            --acr-name "$ACR_NAME" \
            --acr-login-server "$ACR_LOGIN_SERVER" \
            --source-registry-username "$SOURCE_REGISTRY_USERNAME" \
            --source-registry-password "$SOURCE_REGISTRY_PASSWORD" \
            --out "$RUNNER_TEMP/production-release-deploy-state.json"
          promote_status=$?

          smoke_status=1
          if [ "${promote_status}" -eq 0 ]; then
            pnpm --filter @compass/pipeline-tools run release:verify:production -- \
              --manifest "$RUNNER_TEMP/release-candidate.json" \
              --deploy-state "$RUNNER_TEMP/production-release-deploy-state.json" \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
              --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
              --worker-app-name "$AZURE_CONTAINERAPP_WORKER_NAME" \
              --api-base-url "$API_BASE_URL" \
              --web-base-url "$WEB_BASE_URL"
            smoke_status=$?
          fi

          cleanup_status=1
          if [ "${smoke_status}" -eq 0 ]; then
            pnpm --filter @compass/pipeline-tools run release:cleanup:production -- \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
              --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
              --out "$RUNNER_TEMP/production-release-cleanup.json"
            cleanup_status=$?
          fi

          rollback_status=0
          if [ "${promote_status}" -ne 0 ] || [ "${smoke_status}" -ne 0 ]; then
            node pipeline/shared/scripts/azure/set-blue-green-traffic.mjs \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --api-app-name "$AZURE_CONTAINERAPP_API_NAME" \
              --web-app-name "$AZURE_CONTAINERAPP_WEB_NAME" \
              --primary-label "$ACTIVE_LABEL" \
              --primary-weight "100" \
              --secondary-label "$INACTIVE_LABEL" \
              --secondary-weight "0"
            rollback_status=$?
          fi

          verdict="fail"
          if [ "${promote_status}" -eq 0 ] && [ "${smoke_status}" -eq 0 ] && [ "${cleanup_status}" -eq 0 ]; then
            verdict="pass"
          fi

          summary="promote=${promote_status} smoke=${smoke_status} cleanup=${cleanup_status} rollback=${rollback_status} active=${ACTIVE_LABEL} inactive=${INACTIVE_LABEL}"
          echo "verdict=${verdict}" >> "$GITHUB_OUTPUT"
          echo "release_summary=${summary}" >> "$GITHUB_OUTPUT"

      - name: Generate release attestation predicate
        run: |
          predicate_path="${GITHUB_WORKSPACE}/.artifacts/attestations/release-predicate.json"
          mkdir -p "$(dirname "${predicate_path}")"
          pnpm --filter @compass/pipeline-tools run release:predicate:generate -- \
            --out "${predicate_path}" \
            --candidate-id "${{ needs.resolve_candidate.outputs.candidate_id }}" \
            --source-revision "${{ needs.resolve_candidate.outputs.source_revision }}" \
            --verdict "${{ steps.release_run.outputs.verdict }}" \
            --workflow-run-id "${{ github.run_id }}" \
            --released-at "${{ steps.release_start.outputs.released_at }}" \
            --deployment-ref "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Attest release verdict to candidate subject
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26 # v4.1.0
        with:
          subject-name: ${{ env.RELEASE_UNITS_REPO }}
          subject-digest: ${{ steps.release_unit.outputs.release_unit_digest }}
          predicate-type: ${{ env.RELEASE_PREDICATE_TYPE }}
          predicate-path: ${{ github.workspace }}/.artifacts/attestations/release-predicate.json
          push-to-registry: true

      - name: Upload release diagnostics on failure
        if: ${{ !cancelled() && (failure() || steps.release_run.outputs.verdict != 'pass') }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: release-diagnostics-${{ github.run_id }}
          path: |
            .artifacts/release
            .artifacts/attestations
            ${{ runner.temp }}/production-release-deploy-state.json
            ${{ runner.temp }}/production-release-cleanup.json
            ${{ runner.temp }}/blue-green-slot.json
            ${{ runner.temp }}/acceptance-attestation-verification.json
          if-no-files-found: ignore
          retention-days: 7

      - name: Write release summary
        if: ${{ always() }}
        run: |
          {
            echo "## Release Stage"
            echo "- Candidate: ${{ needs.resolve_candidate.outputs.candidate_id }}"
            echo "- Source revision: ${{ needs.resolve_candidate.outputs.source_revision }}"
            echo "- Release unit digest: ${{ steps.release_unit.outputs.release_unit_digest }}"
            echo "- Verdict: ${{ steps.release_run.outputs.verdict }}"
            echo "- Summary: ${{ steps.release_run.outputs.release_summary }}"
            echo "- Previous active slot label: ${{ steps.slot_context.outputs.active_label }}"
            echo "- Promoted slot label: ${{ steps.slot_context.outputs.inactive_label }}"
            echo "- Rehearsal URL: ${{ steps.slot_context.outputs.inactive_web_base_url }}"
            echo "- Production URL: ${{ vars.PRODUCTION_WEB_BASE_URL }}"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Fail release stage when verdict is non-pass
        if: ${{ steps.release_run.outputs.verdict != 'pass' }}
        run: |
          echo "Release failed for candidate ${{ needs.resolve_candidate.outputs.candidate_id }}: ${{ steps.release_run.outputs.release_summary }}" >&2
          exit 1

  release_skip:
    name: Release Stage (skipped)
    needs:
      - resolve_candidate
    if: ${{ needs.resolve_candidate.outputs.run_release != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Record skip
        run: |
          {
            echo "## Release Stage"
            echo "- Candidate: ${{ needs.resolve_candidate.outputs.candidate_id }}"
            echo "- Result: skipped"
            echo "- Reason: ${{ needs.resolve_candidate.outputs.skip_reason }}"
          } >> "$GITHUB_STEP_SUMMARY"
