name: Infra

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/40-infra.yml
      - .github/actions/setup-node-pnpm/action.yml
      - platform/infra/azure/**/*.bicep
      - platform/config/**
      - platform/scripts/infra/**
      - platform/scripts/dev/validate-bicep-templates.mjs
      - platform/pipeline/shared/scripts/azure/az-command.mjs
  push:
    branches:
      - main
    paths:
      - .github/workflows/40-infra.yml
      - .github/actions/setup-node-pnpm/action.yml
      - platform/infra/azure/**/*.bicep
      - platform/config/**
      - platform/scripts/infra/**
      - platform/scripts/dev/validate-bicep-templates.mjs
      - platform/pipeline/shared/scripts/azure/az-command.mjs

permissions:
  contents: read

jobs:
  validate:
    name: Validate Infra
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Node and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Validate Bicep templates
        run: node platform/scripts/dev/validate-bicep-templates.mjs

  apply:
    name: Apply Infra
    if: ${{ github.event_name == 'push' }}
    needs:
      - validate
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      id-token: write
    environment:
      name: production
    env:
      AZURE_DEPLOY_CLIENT_ID: ${{ vars.AZURE_DEPLOY_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      DEPLOYMENT_STAMP: ${{ vars.DEPLOYMENT_STAMP }}
      PRODUCTION_WEB_BASE_URL: ${{ vars.PRODUCTION_WEB_BASE_URL }}
      AUTH_MODE: ${{ vars.AUTH_MODE }}
      ENTRA_WEB_CLIENT_ID: ${{ vars.ENTRA_WEB_CLIENT_ID }}
      ENTRA_ALLOWED_TENANT_IDS: ${{ vars.ENTRA_ALLOWED_TENANT_IDS }}
      AZURE_VNET_ADDRESS_PREFIX: ${{ vars.AZURE_VNET_ADDRESS_PREFIX }}
      AZURE_ACA_SUBNET_PREFIX: ${{ vars.AZURE_ACA_SUBNET_PREFIX }}
      AZURE_POSTGRES_SUBNET_PREFIX: ${{ vars.AZURE_POSTGRES_SUBNET_PREFIX }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Node and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Azure login
        uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2
        with:
          client-id: ${{ env.AZURE_DEPLOY_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Apply platform infrastructure
        run: pnpm infra:apply
