NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
API_BASE_URL=http://localhost:3001

# Optional token for browser and API smoke flows.
NEXT_PUBLIC_BEARER_TOKEN=

# Backward-compatible local development token for test flows.
NEXT_PUBLIC_DEV_BEARER_TOKEN=

# Optional runtime-only token for server-side rendering in production.
BEARER_TOKEN=
