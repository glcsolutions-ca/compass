# Runtime proxy target for /api/v1/* route handlers.
# Required in production. Defaults to http://localhost:3001 in development/test.
API_BASE_URL=http://localhost:3001

# Required in production for signing and validating BFF session cookies.
WEB_SESSION_SECRET=compass-web-session-dev-secret

# Optional comma-separated origin allowlist for CSRF origin/referer checks.
# WEB_ALLOWED_ORIGINS=http://localhost:3000

# Direct codex gateway integration for the homepage prototype.
NEXT_PUBLIC_CODEX_API_BASE_URL=http://localhost:3010
NEXT_PUBLIC_CODEX_WS_BASE_URL=ws://localhost:3010
