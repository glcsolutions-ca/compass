# Production Stage Runbook

This runbook is kept for backward links.

Use the canonical cloud delivery runbook:

- `docs/runbooks/cloud-delivery-pipeline.md`

Key current contracts:

- Push workflow: `.github/workflows/cloud-delivery-pipeline.yml`
- Replay workflow: `.github/workflows/cloud-delivery-replay.yml`
- Release package manifest: `.artifacts/release-package/<sha>/manifest.json`
- Release decision: `.artifacts/release/<sha>/decision.json`
