export const REPOSITORY_SLUG = "glcsolutions-ca/compass";

export const GITHUB_ENVIRONMENT_NAMES = Object.freeze(["stage", "production"]);

export const ENTRA_APP_DISPLAY_NAMES = Object.freeze({
  api: "compass-api-prd",
  web: "compass-web-prd",
  deploy: "compass-deploy-prd"
});

export const ENTRA_REDIRECT_URI_PATH = "/v1/auth/entra/callback";

