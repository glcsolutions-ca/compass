{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "API_PORT",
    "AUTH_MODE",
    "DATABASE_URL",
    "ENTRA_REDIRECT_URI",
    "PORT",
    "POSTGRES_PORT",
    "VITE_API_BASE_URL",
    "WEB_BASE_URL",
    "WEB_PORT",
    "AGENT_GATEWAY_ENABLED",
    "AGENT_DEFAULT_EXECUTION_MODE",
    "AGENT_RUNTIME_PROVIDER",
    "AGENT_RUNTIME_REQUEST_TIMEOUT_MS",
    "AGENT_CLOUD_MODE_ENABLED",
    "AGENT_LOCAL_MODE_ENABLED_DESKTOP",
    "AGENT_MODE_SWITCH_ENABLED",
    "AGENT_CLOUD_DRIVER_MOCK",
    "AGENT_SESSION_CONNECT_SECRET",
    "AGENT_SESSION_BOOTSTRAP_TIMEOUT_MS",
    "AGENT_SESSION_RESPONSE_TIMEOUT_MS",
    "AGENT_SESSION_LOCAL_WORK_ROOT",
    "API_PUBLIC_BASE_URL",
    "IDENTITY_ENDPOINT",
    "IDENTITY_HEADER",
    "MSI_ENDPOINT",
    "MSI_SECRET",
    "DYNAMIC_SESSIONS_POOL_MANAGEMENT_ENDPOINT",
    "DYNAMIC_SESSIONS_BEARER_TOKEN",
    "DYNAMIC_SESSIONS_TOKEN_RESOURCE",
    "DYNAMIC_SESSIONS_EXECUTOR_CLIENT_ID",
    "DYNAMIC_SESSIONS_REQUEST_TIMEOUT_MS"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", "dist-types/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "generate": {
      "dependsOn": ["^generate"],
      "outputs": ["openapi/**", "src/generated/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "test": {
      "outputs": [],
      "outputLogs": "errors-only"
    }
  }
}
